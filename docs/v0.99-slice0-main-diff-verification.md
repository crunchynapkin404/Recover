# v0.99 slice 0 — the constraint, tested against `main` for the first time

**Question.** The branch's governing constraint is _no athlete-visible visual
change_, with four recorded exceptions. Has that been tested? No. The branch's
pixel-diff evidence used `.screenshots/slice0-final/` as its baseline, and that
directory was captured **after** the token commits landed. Every capture on the
branch is post-token, so the diff compared one task's output against another
task's output. It says nothing about the delta from `main`.

This report captures `main` and compares it, surface by surface.

## 1. Exactly what was compared to what

This is the crux, so it is stated before any number.

|                | baseline                                                      | subject                                  |
| -------------- | ------------------------------------------------------------- | ---------------------------------------- |
| commit         | `2da5f83` (`main`)                                            | `9196ecd` (`v0.99-the-app-you-can-read`) |
| worktree       | detached worktree, outside the repo tree                      | `/home/vscode/recover`                   |
| dev server     | `localhost:3200`                                              | `localhost:3100`                         |
| database       | dev Postgres `5435` (`recover-devdb`) — **the same database** | same                                     |
| account        | `demo@recover.local`, owner, seeded                           | same                                     |
| rendered state | `:root`, **no `.dark` class**                                 | `.dark`, applied by `ThemeProvider`      |

**What "dark" means on `main`.** `main` has no `.dark` class anywhere. Its single
`:root` block _is_ the dark theme (`--background: #0a0a0a`), and `next-themes` is
a dependency it never imports. So `main` has exactly **one** renderable state,
and that state is what every athlete on `main` sees.

Two consequences, both load-bearing:

1. **The baseline was captured with no `.dark` class on the document.** `main`
   _does_ already ship `@custom-variant dark (&:is(.dark *))`, so if the capture
   script had toggled `.dark` on — as it does on the branch — it would have
   switched on the 14 `dark:` utilities (21 occurrences, 4 files — count
   corrected by I3) that are dead code on `main`. That would
   have silently erased recorded exception 2 from the diff. The baseline script
   was patched to assert `.dark` is **absent** and that `body` computes
   `rgb(10, 10, 10)`, and to refuse to capture otherwise.
2. **Light mode is not comparable and is excluded.** `main` has no light theme
   to compare against, and on the branch light mode is unreachable by the
   athlete (`forcedTheme="dark"`). There is no athlete-visible light-mode delta
   because there is no athlete-visible light mode. Every number in this report is
   about the one state an athlete can actually reach.

So the comparison is: **`main`'s only rendered state vs the branch's dark state**
— i.e. what an athlete saw before this branch vs what an athlete sees after it.
That is the only comparison the constraint is about.

**The instrument.** `main` has no `scripts/verify-surfaces.ts`; the branch's was
copied in. It is the instrument, not the subject. Its refusal to run without
`SCREENSHOT_BASE_URL` and its refusal of port 3000 were left intact. Four
adaptations were made to the **copy in the main worktree only**, each marked
`BASELINE ADAPTATION` in that file:

1. dropped the `src/lib/design/{contrast,tokens}` imports — branch-only files;
2. `EXPECTED_BODY_BACKGROUND` hardcoded to `rgb(10, 10, 10)`, `main`'s `:root`
   `--background`, since there is no token module to read it from;
3. `forceThemeVerified` **does not toggle the class** and asserts its absence
   (reason 1 above);
4. the theme loop runs once, not twice. Output files are still labelled `-dark-`
   purely so they pair 1:1 with the branch's dark captures by filename.

No source file on the branch was changed. `.screenshots/` is gitignored, so this
report carries the findings rather than PNGs.

## 2. What the CSS says should change (arithmetic, before looking)

The branch touches 28 files; only five can move a pixel: `src/app/globals.css`,
`src/app/layout.tsx`, `src/components/theme-provider.tsx`,
`src/app/train/page.tsx`, `src/components/train/fitness-tiles.tsx`. That bounds
the search.

`main`'s translucent tokens are composited against its own ground to get a
comparable rendered value. White at alpha _a_ over `#0a0a0a` renders as
`a·255 + (1−a)·10`.

| token                                                              | `main` `:root` (rendered)         | branch `.dark`                           | moves?              |
| ------------------------------------------------------------------ | --------------------------------- | ---------------------------------------- | ------------------- |
| `--background`                                                     | `#0a0a0a` rgb(10)                 | `#0a0a0a` rgb(10)                        | no                  |
| `--foreground`                                                     | `#fafafa` rgb(250)                | `#f5f5f5` rgb(245)                       | **yes**             |
| `--card`                                                           | `rgba(255,255,255,.05)` → rgb(22) | `#161616` rgb(22)                        | opacity only        |
| `--card-foreground`                                                | rgb(250)                          | rgb(245)                                 | **yes**             |
| `--popover`                                                        | `#141414` rgb(20)                 | `#1f1f1f` rgb(31)                        | **yes**             |
| `--popover-foreground`                                             | rgb(250)                          | rgb(245)                                 | **yes**             |
| `--secondary`                                                      | `rgba(…,.08)` → rgb(30)           | `#262626` rgb(38)                        | **yes**             |
| `--secondary-foreground`                                           | rgb(250)                          | rgb(245)                                 | **yes**             |
| `--muted`                                                          | `rgba(…,.05)` → rgb(22)           | `#262626` rgb(38)                        | **yes**             |
| `--muted-foreground`                                               | `rgba(…,.4)` → rgb(108)           | `#8a8a8a` rgb(138)                       | **yes**             |
| `--border`                                                         | `rgba(…,.1)` → rgb(34)            | `#6b6b6b` rgb(107)                       | **yes, largest**    |
| `--input`                                                          | `rgba(…,.1)` → rgb(34)            | `#6b6b6b` rgb(107)                       | **yes, largest**    |
| `--sidebar-foreground`                                             | rgb(250)                          | rgb(245)                                 | **yes**             |
| `--sidebar-accent`                                                 | `rgba(…,.08)` → rgb(30)           | `#262626` rgb(38)                        | **yes**             |
| `--sidebar-accent-foreground`                                      | rgb(250)                          | rgb(245)                                 | **yes**             |
| `--sidebar-border`                                                 | `rgba(…,.06)` → rgb(25)           | `#6b6b6b` rgb(107)                       | **yes, largest**    |
| `--primary` / `--accent` / `--ring`                                | `#10b981`                         | `#10b981`                                | no                  |
| `--destructive`                                                    | `#ef4444`                         | `#ef4444`                                | no                  |
| `--chart-1..5`                                                     | unchanged in dark                 | unchanged                                | no                  |
| `--sidebar`                                                        | `#121212`                         | `#121212`                                | no                  |
| `--glass-*`, `--login-input-*`, `--chat-bubble-*`, `--row-divider` | literals                          | `.dark` re-states the identical literals | no                  |
| `--viz-grid`, `--viz-axis`                                         | literals                          | `.dark` re-states identical              | no                  |
| `--viz-muted-ink`                                                  | `rgba(…,.3)` → rgb(84)            | `--ink-muted` rgb(138)                   | **yes (disclosed)** |
| `.label-micro` colour                                              | `rgba(…,.4)` → rgb(108)           | `--ink-muted` rgb(138)                   | **yes (disclosed)** |
| `.nav-dot` bg + glow                                               | `white` rgb(255)                  | `--ink-primary` rgb(245)                 | **yes**             |

Contrast arithmetic on the two that matter most, against `--surface-base`:

- `--border`: rgb(34) on rgb(10) is **1.24:1** — a whisper. rgb(107) on rgb(10)
  is **3.71:1**. Borders become roughly three times as contrasty.
- `--muted-foreground`: rgb(108) on rgb(22) (`--card`) is **3.77:1**; rgb(138) on
  rgb(22) is **5.24:1**. This reproduces the prior review's 3.8→5.2 figure.

`--border` reaches every element through `@layer base { * { @apply border-border } }`,
so it repaints wherever any border-width is set and no explicit colour overrides
it. `.glass` sets its own border from `--glass-border`, which is preserved, so
glass cards are **not** affected — this is why the change is not uniform and why
looking was necessary.

`src/app/layout.tsx` also changes `themeColor` from a single `#0a0a0a` to a
`prefers-color-scheme` pair. That is browser-chrome colour, not page pixels, so
no screenshot can show it; it is analysed in §5.

<!-- EMPIRICAL SECTIONS FOLLOW: populated from the two capture runs. -->

## 3. Runs taken

| run             | code      | server | output                                             | result                                |
| --------------- | --------- | ------ | -------------------------------------------------- | ------------------------------------- |
| `branch-A`      | `9196ecd` | 3100   | 40 PNGs (2 themes × 2 viewports × 10)              | exit 1 (light-mode axe findings only) |
| `main-baseline` | `2da5f83` | 3200   | 20 PNGs (1 state × 2 viewports × 10)               | exit 1 (1 axe finding, see §6)        |
| `branch-A2`     | `9196ecd` | 3100   | 40 PNGs — **noise floor**, same code as `branch-A` | exit 1                                |

`branch-A2` exists because a checksum difference is not evidence of a code
difference until you know what two runs of the _same_ code produce. It turned
out to matter more than anything else in this report.

## 4. Noise floor — measured, not assumed

Same commit, same database, same account, two consecutive runs:

| capture                                                                        | px differing | %        | max Δ |
| ------------------------------------------------------------------------------ | ------------ | -------- | ----- |
| admin-dark-desktop                                                             | 39 015       | 0.537    | 233   |
| admin-dark-phone                                                               | 123 257      | 1.424    | 233   |
| settings-token-created-dark-desktop                                            | 1 558 454    | **44.5** | 245   |
| settings-token-created-dark-phone                                              | 2 327 735    | **51.8** | 245   |
| today-dark-phone                                                               | 379          | 0.029    | 75    |
| body-dark-phone                                                                | 199          | 0.015    | 18    |
| settings-dark-phone                                                            | 124          | 0.009    | 82    |
| train-dark-phone                                                               | 89           | 0.003    | 15    |
| settings-dark-desktop                                                          | 47           | 0.004    | 81    |
| today-dark-desktop                                                             | 24           | 0.002    | 33    |
| activity-log-dark-desktop                                                      | 1            | 0.000    | 1     |
| coach ×2, import ×2, login ×2, train-desktop, body-desktop, activity-log-phone | **0**        | 0.000    | 0     |

Two surfaces are **not comparable by pixels at all**:

- **`admin`** renders live, per-run mutable data (session list, sync-job queue,
  security-event log, surface views). Its self-diff is 39 015 px with maxΔ 233 —
  the _same_ transition families (`22,22,22 ↔ 245,245,245`, `104 ↔ 22`) that
  dominate its main-vs-branch diff. Its 74 065 px main-vs-branch figure is
  therefore mostly data, not code.
- **`settings-token-created`** self-diffs at 44–52% **and changes height between
  two runs of identical code** (2431 → 2595 px), because every run creates
  another API token and the token string differs each time. No pixel percentage
  from this surface means anything. It is assessed by eye only.

For every other surface the noise floor is 0–379 px against main-vs-branch
diffs of 513–8 288 px, so those differences are real signal.

## 5. The rendered pass — re-run at branch HEAD, because HEAD had moved

**Why §§3–4's runs are not the evidence for this section.** Those runs captured
`9196ecd`. The branch is now at `33cd77e`, and two of the commits in between are
**theme commits that change exactly the things §2 says token arithmetic cannot
see**:

- `41c4ede` "keep themeColor static while forcedTheme is in force" — reverts
  `layout.tsx`'s `themeColor` from a `prefers-color-scheme` pair back to a single
  `#0a0a0a`, which is what §2's last paragraph flagged and deferred to this
  section.
- `176361b` "stop declaring color-scheme app-wide until light is reachable" —
  adds `enableColorScheme={false}` to `ThemeProvider`, so `next-themes` stops
  writing `document.documentElement.style.colorScheme`.

Reporting §§3–4's captures as the rendered half would therefore have measured a
branch state that no longer exists, and would have carried the two defects those
commits fixed. Three fresh runs were taken.

| run               | code      | server | output  | result                                 |
| ----------------- | --------- | ------ | ------- | -------------------------------------- |
| `branch-B`        | `33cd77e` | 3100   | 40 PNGs | exit 1 (light-mode axe findings only)  |
| `main-baseline-B` | `2da5f83` | 3200   | 20 PNGs | exit 1 (1 confirmed axe node — see §8) |
| `branch-B2`       | `33cd77e` | 3100   | 40 PNGs | exit 1 — **noise floor at HEAD**       |

### 5.1 Exactly what was compared to what

|                | baseline                                               | subject                                  |
| -------------- | ------------------------------------------------------ | ---------------------------------------- |
| commit         | `2da5f83` (`main`)                                     | `33cd77e` (`v0.99-the-app-you-can-read`) |
| worktree       | detached worktree outside the repo tree                | `/home/vscode/recover`                   |
| dev server     | `localhost:3200`                                       | `localhost:3100`                         |
| database       | dev Postgres `5435` (`recover-devdb`) — **same DB**    | same                                     |
| account        | `demo@recover.local`, owner, seeded — **same account** | same                                     |
| rendered state | `:root`, **no `.dark` class**, asserted absent         | `.dark`, applied by `ThemeProvider`      |
| captures       | 20 PNGs (1 state × 2 viewports × 10 surfaces)          | 40 PNGs; only the 20 `*-dark-*` compared |

`main`'s four load-bearing properties were re-verified in the worktree rather
than taken from §1: `.dark` appears nowhere in `src/` except the
`@custom-variant dark` declaration itself (`globals.css:5`); `color-scheme`
appears nowhere at all; `next-themes` is imported nowhere and
`src/components/theme-provider.tsx` does not exist; `:root` sets
`--background: #0a0a0a`. So `main` has exactly one renderable state and that
state is the baseline.

**Light mode is excluded, and this is what that means.** `main` has no light
theme, and on the branch light mode is unreachable behind `forcedTheme="dark"`.
The branch's 20 `*-light-*` captures were taken (the instrument captures both)
and then **excluded from every number in this report**. There is no
athlete-visible light-mode delta because there is no athlete-visible light mode.

**The instrument.** `main` has no `scripts/verify-surfaces.ts`; the branch's copy
was used. Its refusal to run without `SCREENSHOT_BASE_URL` and its refusal of
port 3000 were both left intact and verified present after adaptation. Four
adaptations were made **to the copy in the main worktree only**, each marked
`BASELINE ADAPTATION n of 4`; a comment-stripped `diff` against the branch's
version confirms those four are the _only_ changes:

1. dropped the `src/lib/design/{contrast,tokens}` imports — branch-only files;
2. `EXPECTED_BODY_BACKGROUND` hardcoded to `rgb(10, 10, 10)`;
3. `forceThemeVerified` **does not toggle `.dark`** and refuses to capture if it
   is present — because `main` already ships `@custom-variant dark`, so adding
   the class would activate the 14 dead `dark:` utilities and silently erase
   recorded exception 2 from the diff;
4. the theme loop runs once. Output keeps the `-dark-` label purely so files pair
   1:1 with the branch's dark captures.

Two further environment facts, recorded because they are part of the
measurement: the worktree needed `node_modules` (hardlink-copied from the repo —
the only dependency difference between the two commits is the `axe-core`
devDependency the instrument itself needs, so the runtime dependency set is
identical), and turbopack rejects a `node_modules` **symlink** that leaves the
project root, which is why a copy was used. No source file on the branch was
changed.

### 5.2 Noise floor, re-measured at HEAD

The §4 floor was measured on `9196ecd`. It was re-measured on `33cd77e`
(`branch-B` vs `branch-B2`, identical code) and **it is materially higher on
four surfaces**, so the §4 figures must not be reused:

| capture                                                                     | §4 floor (`9196ecd`) | floor at HEAD                                                             | %     | maxΔ |
| --------------------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------- | ----- | ---- |
| admin-dark-phone                                                            | 123 257              | **150 010**                                                               | 1.733 | 233  |
| admin-dark-desktop                                                          | 39 015               | **48 682**                                                                | 0.671 | 233  |
| settings-dark-phone                                                         | 124                  | **3 089**                                                                 | 0.235 | 187  |
| body-dark-phone                                                             | 199                  | **943**                                                                   | 0.072 | 51   |
| settings-dark-desktop                                                       | 47                   | **744**                                                                   | 0.057 | 85   |
| today-dark-phone                                                            | 379                  | 416                                                                       | 0.032 | 120  |
| today-dark-desktop                                                          | 24                   | 156                                                                       | 0.012 | 117  |
| train-dark-phone                                                            | 89                   | 109                                                                       | 0.003 | 23   |
| activity-log ×2, body-desktop, coach ×2, import ×2, login ×2, train-desktop | 0–1                  | **0**                                                                     | 0.000 | 0    |
| settings-token-created ×2                                                   | 44–52 %              | **height changed on identical code** (desktop 3333→3497, phone 7914→8306) | —     | —    |

Two surfaces remain **not honestly pixel-comparable**, for the reasons §4 gives
and which reproduced exactly:

- **`admin`** — renders live session / sync-job / security-event data. Its floor
  is 48 682 px (desktop) and 150 010 px (phone) in the _same_ transition
  families that dominate its main-vs-branch diff. Against a main-vs-branch
  figure of 76 838 / 283 465 px, **most of that is data, not code.** No
  conclusion about `admin` is drawn from its pixel count.
- **`settings-token-created`** — mints a fresh API token per run, so the token
  string and the page height both change between runs of identical code. Its
  main-vs-branch "dimension mismatch" is therefore **not** evidence of a
  branch-induced layout change; the same mismatch appears between two runs of
  the branch against itself. Assessed by eye only.
