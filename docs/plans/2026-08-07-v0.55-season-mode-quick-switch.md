# v0.55 Plan: Season Mode Quick Switch

Date: 2026-08-07
Scope: One focused implementation PR

## Objective

Deliver a Train-native season mode switch and re-entry starter using existing deterministic planning behavior and existing update pathways.

## In Scope

- Week header season-state chip/switch.
- Server action wrapper for season mode changes.
- Re-entry trigger from Train.
- Week refresh after update.
- Focused tests for UI and action behavior.

## Out of Scope

- Any planning algorithm change.
- New season modes or re-entry stages.
- Schema or migration work.

## Implementation Tasks

1. Add season state switch component in Train.

- Likely files:
  - `src/components/train/season-mode-switch.tsx`
  - `src/components/train/season-mode-switch.test.tsx`

2. Add thin server action wrapper around existing planning update tool.

- Likely files:
  - `src/app/plan/actions.ts`
  - `src/app/plan/actions.test.ts`

3. Wire the component into Train week header.

- Likely files:
  - `src/app/train/page.tsx`

4. Verify re-entry gating behavior.

- Only show `Start re-entry` when off-season and not already in re-entry.

5. Run focused and full verification.

## Verification

1. `npm run format:check`
2. `npm run typecheck`
3. Focused tests for new action/component
4. `npm test`
5. `npm run build`

## Rollout

- Ship as normal release PR.
- Release notes: Train can now switch season mode and start re-entry directly.

## Rollback

- Revert Train season-mode UI and action commits.
- Keep off-season/re-entry backend support unchanged.
